# ---------------- Server env example ----------------
# Rename this file to `server/.env` (or copy/paste its contents) and fill in values.

# Server port (default: 4000)
PORT=4000

# OpenAI (required for grading/evaluation)
OPENAI_API_KEY=

# Admin login (change these in production)
ADMIN_PASSWORD=adminpass
ADMIN_SECRET=verysecret

# Upload limits for multer (in MB). Reverse proxies may still impose lower limits.
MAX_UPLOAD_MB=100

# Limit concurrent Whisper/LLM evaluations to keep uploads stable under load.
# Recommended: 1-3 depending on your CPU and network.
MAX_CONCURRENT_ANALYSIS=2

# ffmpeg support:
# - If ffmpeg is installed and in PATH, the server will use it for better media assembly and audio extraction.
# - To disable all ffmpeg usage, set:
# ENABLE_FFMPEG=false

# Optional: Cloudinary (enables remote video URL storage).
# Prefer CLOUDINARY_URL if you have it; otherwise set the 3 vars below.
# CLOUDINARY_URL=
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=


