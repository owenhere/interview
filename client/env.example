# ---------------- Client env example ----------------
# Rename this file to `client/.env` (or `client/.env.local`) before running/building the frontend.
# The esbuild scripts read `.env.local` first, then `.env`, from inside `client/`.

# Frontend dev server port (default: 5173)
PORT=5173

# Backend origin.
# - Dev (separate ports): "http://localhost:4000"
# - Production behind same origin: leave empty (API_BASE=)
API_BASE=http://localhost:4000

# Backend route prefix used by the server (default: /backend)
API_PREFIX=/backend


